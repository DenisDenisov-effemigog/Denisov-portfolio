.header__container{
  display: grid;
  grid-template: 
  "logo nav icons" 1fr / 
  1fr 3fr 1fr;
  justify-items: center;
  padding-top: 65px;
}
.logo{
  font-size: 36px;
  font-weight: 900;
  text-transform: uppercase;
  grid-area: logo;
}
